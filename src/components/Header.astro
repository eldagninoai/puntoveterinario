---
const currentPath = Astro.url.pathname;

const servicios = [
  {
    href: "/servicios/consulta-veterinaria",
    label: "Consulta Médica",
    emoji: "🩺",
  },
  { href: "/servicios/estetica-canina", label: "Estética Canina", emoji: "🛁" },
  { href: "/servicios/esterilizacion", label: "Esterilización", emoji: "✂️" },
  { href: "/servicios/cirugia-veterinaria", label: "Cirugías", emoji: "🏥" },
  { href: "/servicios/laboratorio-clinico", label: "Laboratorio", emoji: "🔬" },
  { href: "/servicios/alimento-mascotas", label: "Alimento", emoji: "🛒" },
];

const serviciosActivo = currentPath.startsWith("/servicios");
---

<header class="header">
  <div class="container header__inner">
    <a href="/" class="logo" aria-label="Inicio">
      🐾 <span>Punto Veterinario</span>
    </a>

    <div class="nav-overlay" id="nav-overlay"></div>

    <nav class="nav-menu" id="nav-menu" aria-label="Navegación principal">
      <ul class="nav-list">
        <li>
          <a href="/" class={`nav-link ${currentPath === "/" ? "active" : ""}`}>
            Inicio
          </a>
        </li>

        <!-- SERVICIOS: un solo elemento, funciona en desktop y mobile -->
        <li class="nav-item-dropdown">
          <button
            class={`nav-link dropdown-trigger ${serviciosActivo ? "active" : ""}`}
            aria-expanded="false"
            aria-haspopup="true"
            id="servicios-btn"
          >
            Servicios <span class="chevron">▾</span>
          </button>
          <ul class="dropdown-menu" id="dropdown-menu" role="menu">
            {
              servicios.map((s) => (
                <li role="none">
                  <a
                    href={s.href}
                    role="menuitem"
                    class={`dropdown-link ${currentPath === s.href ? "active" : ""}`}
                  >
                    <span class="dropdown-emoji">{s.emoji}</span>
                    {s.label}
                  </a>
                </li>
              ))
            }
          </ul>
        </li>

        <li>
          <a
            href="/nosotros"
            class={`nav-link ${currentPath === "/nosotros" ? "active" : ""}`}
          >
            Nosotros
          </a>
        </li>
        <li>
          <a
            href="/ubicacion"
            class={`nav-link ${currentPath === "/ubicacion" ? "active" : ""}`}
          >
            Ubicación
          </a>
        </li>
        <li>
          <a
            href="/blog"
            class={`nav-link ${currentPath.startsWith("/blog") ? "active" : ""}`}
          >
            Blog
          </a>
        </li>
      </ul>
    </nav>

    <div class="header-actions">
      <a href="/contacto" class="btn-cta">📅 Agendar Cita</a>
      <button
        class="nav-toggle"
        aria-label="Abrir menú"
        aria-expanded="false"
        id="nav-toggle"
      >
        <span></span><span></span><span></span>
      </button>
    </div>
  </div>
</header>

<style>


  .header {
    position: sticky;
    top: 0;
    z-index: 100;
    background: #fff;
    border-bottom: 0.1rem solid var(--color-border);
    padding-block: max(0.6rem, 1vw);
  }

  .header__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: max(1rem, 2vw);
  }

  /* LOGO */
  .logo {
    display: flex;
    align-items: center;
    gap: max(0.3rem, 0.5vw);
    text-decoration: none;
    font-size: max(1rem, 1.6vw);
    font-weight: 700;
    color: var(--color-primary);
    white-space: nowrap;
    flex-shrink: 0;
  }

  /* NAV */
  .nav-menu {
    flex: 1;
    display: flex;
    justify-content: center;
  }
  .nav-list {
    display: flex;
    list-style: none;
    gap: max(0.2rem, 0.5vw);
    align-items: center;
    justify-content: center;
    margin: 0;
    padding: 0;
  }

  /* NAV LINKS */
  .nav-link {
    display: flex;
    align-items: center;
    gap: 0.3em;
    text-decoration: none;
    color: var(--color-text);
    font-size: max(0.8rem, 1vw);
    font-weight: 500;
    padding: max(0.35rem, 0.5vw) max(0.5rem, 0.8vw);
    border-radius: var(--radius-sm);
    transition:
      background 0.2s,
      color 0.2s;
    white-space: nowrap;
    cursor: pointer;
    background: none;
    border: none;
    font-family: var(--font-main);
  }
  .nav-link:hover {
    background: var(--color-primary-light);
    color: var(--color-primary-dark);
  }
  .nav-link.active {
    background: var(--color-primary-light);
    color: var(--color-primary-dark);
    font-weight: 600;
  }
  .chevron {
    color: inherit;
    font-size: 0.7em;
    transition: transform 0.2s;
    display: inline-block;
    opacity: 0.7;
  }
  .dropdown-trigger[aria-expanded="true"] .chevron {
    transform: rotate(180deg);
  }

  /* DROPDOWN */
  .nav-item-dropdown {
    position: relative;
  }
  .dropdown-menu {
    position: absolute;
    top: calc(100% + max(0.3rem, 0.5vw));
    left: 50%;
    transform: translateX(-50%) translateY(-0.5rem);
    background: #fff;
    list-style: none;
    padding: max(0.4rem, 0.6vw);
    margin: 0;
    min-width: max(13rem, 16vw);
    border-radius: var(--radius-lg);
    box-shadow: 0 max(0.3rem, 0.5vw) max(1rem, 1.5vw)
      rgba(0, 0, 0, 0.12);
    border: 0.1rem solid var(--color-border);
    z-index: 200;
    /* Oculto por defecto */
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transition:
      opacity 0.2s ease,
      transform 0.2s ease,
      visibility 0.2s;
  }
  .dropdown-menu.open {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
    transform: translateX(-50%) translateY(0);
  }
  .dropdown-link {
    display: flex;
    align-items: center;
    gap: max(0.4rem, 0.6vw);
    text-decoration: none;
    color: var(--color-text);
    font-size: max(0.8rem, 1vw);
    padding: max(0.4rem, 0.6vw) max(0.6rem, 1vw);
    border-radius: var(--radius-sm);
    transition:
      background 0.15s,
      color 0.15s;
    white-space: nowrap;
  }
  .dropdown-link:hover {
    background: var(--color-primary-light);
    color: var(--color-primary-dark);
  }
  .dropdown-link.active {
    background: var(--color-primary-light);
    color: var(--color-primary-dark);
    font-weight: 600;
  }
  .dropdown-emoji {
    font-size: 1em;
  }

  /* CTA */
  .header-actions {
    display: flex;
    align-items: center;
    gap: max(0.5rem, 1vw);
    flex-shrink: 0;
  }
  .btn-cta {
    background: var(--color-primary);
    color: #fff;
    text-decoration: none;
    font-size: max(0.8rem, 1vw);
    padding: max(0.35rem, 0.5vw) max(0.7rem, 1.2vw);
    border-radius: var(--radius);
    font-weight: 700;
    white-space: nowrap;
    transition:
      filter 0.2s,
      transform 0.2s;
  }
  .btn-cta:hover {
    filter: brightness(1.08);
    transform: translateY(-0.1rem);
  }

  /* HAMBURGER — oculto en desktop */
  .nav-toggle {
    display: none;
  }
  .nav-overlay {
    display: none;
  }

  /* ===== MOBILE ===== */
  @media (
      flex-direction: column;
      justify-content: center;
      gap: 0.3rem;
      background: none;
      border: none;
      cursor: pointer;
      padding: 0.4rem;
      z-index: 102;
    }
    .nav-toggle span {
      display: block;
      width: 1.5rem;
      height: 0.15rem;
      background: var(--color-primary);
      border-radius: 1rem;
      transition: all 0.3s;
    }
    .nav-toggle.open span:nth-child(1) {
      transform: translateY(0.45rem) rotate(45deg);
    }
    .nav-toggle.open span:nth-child(2) {
      opacity: 0;
    }
    .nav-toggle.open span:nth-child(3) {
      transform: translateY(-0.45rem) rotate(-45deg);
    }

    .nav-overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.4);
      z-index: 100;
      opacity: 0;
      visibility: hidden;
      transition:
        opacity 0.3s,
        visibility 0.3s;
    }
    .nav-overlay.active {
      display: block;
      opacity: 1;
      visibility: visible;
    }

    .nav-menu {
      position: fixed;
      top: 0;
      right: -100%;
      bottom: 0;
      width: min(75vw, 20rem);
      background: #fff;
      z-index: 101;
      flex-direction: column;
      justify-content: flex-start;
      padding: 4rem 1.5rem 2rem;
      box-shadow: -0.2rem 0 1.5rem rgba(0, 0, 0, 0.1);
      transition: right 0.3s ease;
      overflow-y: auto;
    }
    .nav-menu.open {
      right: 0;
    }

    .nav-list {
      flex-direction: column;
      align-items: stretch;
      gap: 0.3rem;
      width: 100%;
    }
    .nav-link {
      width: 100%;
      font-size: 1rem;
      padding: 0.7rem 0.8rem;
      justify-content: space-between;
    }

    /* Dropdown en mobile — se abre inline */
    .nav-item-dropdown {
      width: 100%;
    }
    .dropdown-menu {
      position: static;
      transform: none;
      box-shadow: none;
      border: none;
      border-left: 0.2rem solid var(--color-primary-light);
      border-radius: 0;
      padding: 0.3rem 0 0.3rem 1rem;
      min-width: unset;
      width: 100%;
      margin-top: 0.3rem;
      background: transparent;
    }
    .dropdown-menu.open {
      transform: none;
    }
    .dropdown-link {
      font-size: 0.95rem;
      padding: 0.5rem 0.6rem;
    }
  }

    @media (
        }
    }
    @media (
        }
    }
</style>

<script is:inline>
  document.addEventListener("DOMContentLoaded", () => {
    const toggle = document.getElementById("nav-toggle");
    const nav = document.getElementById("nav-menu");
    const overlay = document.getElementById("nav-overlay");
    const serviciosBtn = document.getElementById("servicios-btn");
    const dropdownMenu = document.getElementById("dropdown-menu");

    // ── Menú mobile ──
    function closeMenu() {
      nav?.classList.remove("open");
      toggle?.classList.remove("open");
      overlay?.classList.remove("active");
      toggle?.setAttribute("aria-expanded", "false");
    }
    function openMenu() {
      nav?.classList.add("open");
      toggle?.classList.add("open");
      overlay?.classList.add("active");
      toggle?.setAttribute("aria-expanded", "true");
    }
    toggle?.addEventListener("click", () => {
      nav?.classList.contains("open") ? closeMenu() : openMenu();
    });
    overlay?.addEventListener("click", closeMenu);

    // Cerrar mobile al hacer clic en links (excepto el botón de servicios)
    nav?.querySelectorAll("a").forEach((link) => {
      link.addEventListener("click", closeMenu);
    });

    // ── Dropdown servicios ──
    function closeDropdown() {
      dropdownMenu?.classList.remove("open");
      serviciosBtn?.setAttribute("aria-expanded", "false");
    }
    function openDropdown() {
      dropdownMenu?.classList.add("open");
      serviciosBtn?.setAttribute("aria-expanded", "true");
    }
    function toggleDropdown() {
      dropdownMenu?.classList.contains("open")
        ? closeDropdown()
        : openDropdown();
    }

    serviciosBtn?.addEventListener("click", (e) => {
      e.stopPropagation();
      toggleDropdown();
    });

    // Cerrar dropdown al hacer clic fuera
    document.addEventListener("click", (e) => {
      if (
        !serviciosBtn?.contains(e.target) &&
        !dropdownMenu?.contains(e.target)
      ) {
        closeDropdown();
      }
    });

    // Cerrar dropdown al presionar Escape
    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape") closeDropdown();
    });
  });
</script>
